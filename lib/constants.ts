// Textos de domínio da correção — definidos em `@starguard/core/constants`.
// Ficam no núcleo porque quem os usa é o corretor, que roda nos três produtos;
// o endereço antigo permanece para `FixModal` e `VulnerabilityCard`.
export * from "@starguard/core/constants";
